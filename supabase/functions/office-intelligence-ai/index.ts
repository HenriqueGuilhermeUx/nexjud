import { serve } from "https://deno.land/std@0.224.0/http/server.ts"
import { createClient } from "https://esm.sh/@supabase/supabase-js@2"
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"}
serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const auth=req.headers.get("Authorization")||"";const sb=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_ANON_KEY")!,{global:{headers:{Authorization:auth}}});const{data:{user}}=await sb.auth.getUser();if(!user)throw new Error("Não autenticado");const body=await req.json().catch(()=>({}));const question=String(body.question||"").trim();const[cr,mr,or,dr,ir]=await Promise.all([sb.from("legal_cases").select("id,title,status,risk_level,success_probability,court,tags,created_at").eq("user_id",user.id).limit(300),sb.from("legal_strategic_memories").select("case_id,area,issue,situation,strategy,result_status,result_summary,lesson,confidence_score,reusable,created_at").eq("user_id",user.id).order("created_at",{ascending:false}).limit(200),sb.from("legal_case_outcomes").select("case_id,recommendation,lawyer_decision,action_taken,outcome,outcome_type,result_status,learned_lesson,created_at").eq("user_id",user.id).order("created_at",{ascending:false}).limit(500),sb.from("legal_case_dossiers").select("case_id,executive_summary,legal_issues,evidence_map,precedent_map,risks,strategy,next_best_actions,confidence_score,last_analyzed_at").eq("user_id",user.id).limit(300),sb.from("legal_case_precedent_impacts").select("*").eq("user_id",user.id).order("created_at",{ascending:false}).limit(200)]);for(const r of[cr,mr,or,dr,ir])if(r.error)throw r.error;const cases=cr.data||[],memories=mr.data||[],outcomes=or.data||[],dossiers=dr.data||[],impacts=ir.data||[];const caseMap=Object.fromEntries(cases.map((c:any)=>[c.id,c]));const gaps=dossiers.flatMap((d:any)=>(d.evidence_map||[]).filter((e:any)=>Boolean(e?.gap)).map((e:any)=>({case_id:d.case_id,case_title:caseMap[d.case_id]?.title,requirement:e.requirement,gap:e.gap,recommended_action:e.recommended_action})));const risks=dossiers.flatMap((d:any)=>(d.risks||[]).map((r:any)=>({case_id:d.case_id,case_title:caseMap[d.case_id]?.title,...r})));const critical=risks.filter((r:any)=>/alto|high|crit/i.test(String(r.level||""))).slice(0,20);const context={metrics:{cases:cases.length,dossiers:dossiers.length,evidence_gaps:gaps.length,high_risks:critical.length,memories:memories.length,outcomes:outcomes.filter((o:any)=>o.outcome).length,precedent_impacts:impacts.length},cases,dossiers,critical_risks:critical,evidence_gaps:gaps.slice(0,60),strategic_memories:memories.filter((m:any)=>m.reusable).slice(0,50),recent_outcomes:outcomes.slice(0,80),precedent_impacts:impacts.slice(0,50)};const key=Deno.env.get("OPENAI_API_KEY");let result:any;if(key){const task=question?`Responda à pergunta do gestor: ${question}`:"Produza o briefing executivo do escritório para hoje.";const prompt=`Você é o Office Intelligence Agent do NexJud, um assessor executivo jurídico. ${task} Use SOMENTE os dados fornecidos. Não invente prazos, processos, decisões, precedentes, probabilidades ou causalidade. Memória estratégica é experiência operacional, não autoridade jurídica. Priorize risco, lacunas probatórias, impactos de precedentes e ações concretas. Sempre associe recomendação ao case_id quando houver. Retorne JSON estrito: answer string, executive_summary string, critical_cases [{case_id,title,reason,priority}], evidence_alerts [{case_id,title,gap,action}], precedent_alerts [{case_id,title,reason,action}], reusable_lessons [{case_id,lesson,why_relevant}], priority_actions [{case_id,title,action,why,priority}], confidence_score 0-100. DADOS=${JSON.stringify(context).slice(0,120000)}`;const r=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-4o-mini",response_format:{type:"json_object"},temperature:.1,messages:[{role:"system",content:"Você é um agente executivo jurídico conservador, auditável e orientado a ação."},{role:"user",content:prompt}]})});if(!r.ok)throw new Error(`OpenAI ${r.status}: ${await r.text()}`);const ai=await r.json();result=JSON.parse(ai.choices?.[0]?.message?.content||"{}")}else{result={answer:"Office Intelligence carregado em modo estrutural.",executive_summary:`${cases.length} casos, ${critical.length} riscos altos e ${gaps.length} lacunas probatórias identificadas.`,critical_cases:critical.slice(0,5).map((x:any,i:number)=>({case_id:x.case_id,title:x.case_title||"Caso",reason:x.risk||x.reason||"Risco relevante",priority:i+1})),evidence_alerts:gaps.slice(0,5).map((x:any)=>({case_id:x.case_id,title:x.case_title||"Caso",gap:x.gap||x.requirement,action:x.recommended_action||"Revisar prova"})),precedent_alerts:[],reusable_lessons:memories.filter((m:any)=>m.reusable).slice(0,5).map((m:any)=>({case_id:m.case_id,lesson:m.lesson,why_relevant:m.issue||m.area||"Experiência institucional"})),priority_actions:[],confidence_score:30}}return new Response(JSON.stringify(result),{headers:{...cors,"Content-Type":"application/json"}})}catch(e){return new Response(JSON.stringify({error:e instanceof Error?e.message:String(e)}),{status:400,headers:{...cors,"Content-Type":"application/json"}})}})